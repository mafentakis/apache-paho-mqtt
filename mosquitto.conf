# Mosquitto configuration for development
# This configuration allows anonymous connections and binds to all interfaces

# Listen on port 1883 on all available interfaces
listener 1883 0.0.0.0

# Allow connections without authentication (development only!)
allow_anonymous true

# Disable persistence for development (optional)
persistence false

# Log to stdout for easier debugging with docker logs
log_dest stdout
log_type all

